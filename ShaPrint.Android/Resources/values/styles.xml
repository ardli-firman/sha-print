<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash theme referenced by MainActivity (Theme = "@style/MyTheme.Splash"). Light
         Material theme + the app icon as window background so the launch shows the ShaPrint
         icon instead of a blank white screen. -->
    <style name="MyTheme.Splash" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_launcher</item>
    </style>
</resources>
